import { styled } from "styled-components";

export const BookingModalHeaderImage = styled.img`
  aspect-ratio: 16/9;
  border-radius: var(--lg);
  object-fit: cover;
  position: relative;
  width: 100%;
`;
